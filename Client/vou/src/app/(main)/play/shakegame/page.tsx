const page = ({
	searchParams,
}: {
	searchParams: {
		brand: string;
		event: string;
		shakegame: string;
	};
}) => {
	const { brand, event, shakegame } = searchParams;
	return <div></div>;
};

export default page;
